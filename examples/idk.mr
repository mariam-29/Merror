# full_test.mr — exercises all Merror constructs

# hello.mr — basic Merror demo

x = "iam mariam";
z = 7;
name = "Merror";

fi (z > 5) {
    x = 3 + 5;
    tnirp(rts(x));
}

tnirp("Hello from " + name);